- Make sure the same id doesn't appear multiple times in the tree
- Make sure children are ordered ? (important for arithmetic)
- Can only have one param with dimensions for functions??? check FPARAMS3 grammar rule